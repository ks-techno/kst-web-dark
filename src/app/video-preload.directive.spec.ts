import { VideoPreloadDirective } from './video-preload.directive';

describe('VideoPreloadDirective', () => {
  it('should create an instance', () => {
    const directive = new VideoPreloadDirective();
    expect(directive).toBeTruthy();
  });
});
